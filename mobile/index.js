/**
 * @format
 */

import { AppRegistry } from 'react-native';
import App from './App';

// Expo expects the app to be registered with the name 'main'
AppRegistry.registerComponent('main', () => App);
